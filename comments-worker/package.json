{
  "name": "kuailu-comments-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --config wrangler.jsonc && tsc --noEmit",
    "dev": "wrangler dev --config wrangler.jsonc --local",
    "deploy": "wrangler deploy --config wrangler.jsonc --minify",
    "migrate:local": "wrangler d1 migrations apply kuailu-comments --config wrangler.jsonc --local",
    "migrate:remote": "wrangler d1 migrations apply kuailu-comments --config wrangler.jsonc --remote"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "wrangler": "4.127.0"
  }
}
