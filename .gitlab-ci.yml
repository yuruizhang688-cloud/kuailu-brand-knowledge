image: node:24-alpine

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm ci
    - export VITE_BASE_PATH="$(node build/gitlab-pages-base.js)"
    - npm run build:gitlab
  pages:
    publish: dist
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
